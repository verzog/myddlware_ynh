#!/bin/bash
set -eu

source /usr/share/yunohost/helpers
source "$(dirname "$0")/_common.sh"

#=================================================
# PRESERVE .env.local
#=================================================
tmp_env=$(mktemp)
if [ -f "$install_dir/.env.local" ]; then
    cp "$install_dir/.env.local" "$tmp_env"
fi

#=================================================
# UPDATE SOURCES
#=================================================
ynh_script_progression --message="Updating source files" --weight=2
ynh_setup_source --dest_dir="$install_dir"

if [ -s "$tmp_env" ]; then
    cp "$tmp_env" "$install_dir/.env.local"
    chmod 640 "$install_dir/.env.local"
fi
rm -f "$tmp_env"

chown -R "$app:$app" "$install_dir"

#=================================================
# UPDATE NGINX / PHP-FPM
#=================================================
ynh_script_progression --message="Refreshing PHP-FPM pool"
ynh_add_fpm_config --phpversion="$phpversion"

ynh_script_progression --message="Refreshing NGINX configuration"
ynh_add_nginx_config

#=================================================
# COMPOSER
#=================================================
ynh_script_progression --message="Updating Composer dependencies" --weight=10
ynh_install_composer --phpversion="$phpversion" --workdir="$install_dir" --composerversion=2.7.7
# NB: keep dev deps — Myddleware loads DoctrineFixturesBundle in prod, so
# --no-dev breaks kernel boot (see install script for details).
ynh_composer_exec --phpversion="$phpversion" --workdir="$install_dir" \
    --commands="install --optimize-autoloader"

#=================================================
# PATCH FRONTEND ASSETS
#=================================================
# 1. single jQuery (alias) so jquery-ui widgets attach to the app's instance;
# 2. require jQuery UI early in 'app' so $.fn.draggable exists before any ready
#    handler (no CDN); 3. absolute publicPath so assets resolve on deep routes.
# See install script for the full rationale.
ynh_script_progression --message="Patching frontend assets"

if ! grep -q "ynh: bundle jQuery UI" "$install_dir/assets/app.js"; then
    tmp_appjs="$(mktemp)"
    cat > "$tmp_appjs" <<'JS'
// ynh: bundle jQuery UI early, against the single aliased jQuery
require('jquery-ui');
require('jquery-ui/ui/widgets/draggable');
require('jquery-ui/ui/widgets/droppable');
require('jquery-ui/ui/widgets/sortable');
require('jquery-ui/ui/widgets/resizable');
require('jquery-ui/ui/widgets/dialog');
require('jquery-ui/ui/widgets/tabs');
require('jquery-ui/ui/widgets/accordion');
JS
    cat "$install_dir/assets/app.js" >> "$tmp_appjs"
    mv "$tmp_appjs" "$install_dir/assets/app.js"
    chown "$app:$app" "$install_dir/assets/app.js"
fi

if ! grep -q "addAliases({ jquery:" "$install_dir/webpack.config.js"; then
    sed -i "s#setManifestKeyPrefix('build/')#setManifestKeyPrefix('build/')\n    .addAliases({ jquery: require.resolve('jquery') })#" "$install_dir/webpack.config.js"
fi

public_path="${path%/}/build"
sed -i "s#setPublicPath('./build')#setPublicPath('$public_path')#" "$install_dir/webpack.config.js"

#=================================================
# NODE.JS + FRONTEND ASSETS
#=================================================
ynh_script_progression --message="Installing Node.js" --weight=10
ynh_install_nodejs --nodejs_version="$nodejs_version"
ynh_use_nodejs

# Yarn (classic) is not bundled with Node; (re)install it into the Node prefix.
ynh_exec_warn_less npm install -g yarn

# Run yarn with the process CWD and HOME inside the (app-owned) install dir to
# avoid Yarn Classic's .yarnrc walk hitting root-only /var/cache/yunohost (EACCES).
ynh_script_progression --message="Installing JS dependencies (yarn)" --weight=15
(cd "$install_dir" && sudo -u "$app" env PATH="$PATH" HOME="$install_dir" yarn install --frozen-lockfile)

ynh_script_progression --message="Building frontend assets (yarn build)" --weight=15
(cd "$install_dir" && sudo -u "$app" env PATH="$PATH" HOME="$install_dir" yarn build)

# Fail loudly if the build silently produced nothing.
if [ ! -f "$install_dir/public/build/entrypoints.json" ]; then
    ynh_die --message="Asset build failed: public/build/entrypoints.json was not generated"
fi

chown -R "$app:$app" "$install_dir"

#=================================================
# DATABASE MIGRATIONS
#=================================================
ynh_script_progression --message="Running database migrations" --weight=5
if [ -x "$install_dir/bin/console" ]; then
    sudo -u "$app" php$phpversion "$install_dir/bin/console" \
        doctrine:migrations:migrate --no-interaction
fi

#=================================================
# DUMP JS ROUTES
#=================================================
ynh_script_progression --message="Dumping JS routes"
# Run from the install dir (CWD) and create the target dir as the app user, since
# fos:js-routing:dump resolves the relative target against the process CWD.
sudo -u "$app" mkdir -p "$install_dir/public/js"
(cd "$install_dir" && sudo -u "$app" php$phpversion "$install_dir/bin/console" \
    fos:js-routing:dump --format=json --target=public/js/fos_js_routes.json)

chown -R "$app:$app" "$install_dir"

#=================================================
# RELOAD SERVICES
#=================================================
ynh_script_progression --message="Reloading services"
ynh_systemd_action --service_name="php${phpversion}-fpm" --action=reload
ynh_systemd_action --service_name=nginx --action=reload
