#!/bin/bash

source /usr/share/yunohost/helpers

ynh_abort_if_errors

# This app does not expose additional configuration hooks beyond the
# automatically generated panel. The call below keeps compatibility with
# the config panel system.
ynh_app_config_run "$1"
