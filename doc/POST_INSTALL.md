Myddleware is now available at https://__DOMAIN____PATH__

An administrator account was created automatically during installation:

- Username: `__ADMIN_USERNAME__`
- Email: `__ADMIN__`
- Password: `__ADMIN_PASSWORD__`

You can log in with either the username or the email address. Please change
this password after your first login.

If you ever need these credentials again, retrieve them on the server with:

```
yunohost app setting myddleware admin_username
yunohost app setting myddleware admin_password
```
