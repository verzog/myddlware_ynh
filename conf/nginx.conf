
location __PATH__/ {
    alias __INSTALL_DIR__/public/;
    try_files $uri /index.php?$args;
}

location ~ \.php$ {
    include fastcgi_params;
    fastcgi_pass unix:/run/php/php8.4-fpm-__APP__.sock;
    fastcgi_param SCRIPT_FILENAME $request_filename;
}
